Satoshi is not bundled — it is a licensed font and is not redistributable here.

To use it:
  1. Get Satoshi-Variable.woff2 from your Fontshare/Indian Type Foundry licence
  2. Drop it in this folder, keeping the exact filename
  3. Reload the extension

Until then the panel falls back to the system sans-serif. Nothing else breaks.
