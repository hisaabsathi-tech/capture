Courier logos (optional).

Drop these two files here and the header will show the real logo instead of a
text badge, based on which courier tab you are on:

  assets/ekart.png
  assets/shree-maruti.png

Use a transparent PNG about 20px tall (40px for retina). If a file is missing,
the text badge is shown instead.
